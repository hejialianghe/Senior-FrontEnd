const sub = {
  'firstname_1_1':"jl",
  'lastname_2_1':"h"
}
const result = []
for(item of Object.keys(sub)) {
  const keys = item.split('_')
  if(result[keys[1]]){
    result[keys[1]]=[{
      firstname:sub[fis]
      lastname:
    }]
  }
    const arr = []
  console.log(result[keys[0]])
}